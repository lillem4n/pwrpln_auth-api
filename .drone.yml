kind: pipeline
type: docker
name: default

steps:
  - name: Tests
    image: docker/compose:1.29.2
    commands:
      - docker-compose run --rm db-migrations
      - docker-compose up -d
      - docker-compose run --rm tests
